class Solution:
    def networkDelayTime(self, times: List[List[int]], n: int, k: int) -> int:
        # graph = { from_node: [(to_nodes, time)]}
        # times = [math.inf for count]
        # times[from] = 0

        # heap = [(0, start)]
        # while heap:
        # target, dis
        #  if times[target] < dis:
        #     continue
        #     for times[target]
        #         next
                
        #         heapq.heappush()
